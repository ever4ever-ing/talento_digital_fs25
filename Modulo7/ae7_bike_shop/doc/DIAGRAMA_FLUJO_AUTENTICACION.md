# 🔄 Flujo de Autenticación y Permisos

## Diagrama de Flujo - Usuario No Autenticado

```
┌─────────────────────────────┐
│  Usuario Llega a la App     │
│  GET /                      │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  ¿Autenticado?              │
│  user.is_authenticated      │
└────────┬────────────────────┘
         │ NO
         ▼
┌─────────────────────────────┐
│  Muestra:                   │
│  - Catálogo (lectura)       │
│  - Link "Registrarse"       │
│  - Link "Iniciar Sesión"    │
│  - Sin botones Editar       │
│  - Sin botones Eliminar     │
└─────────────────────────────┘
```

---

## Diagrama de Flujo - Registro

```
┌──────────────────────────────┐
│  Usuario va a /auth/registro/│
└────────────┬─────────────────┘
             │
             ▼
┌──────────────────────────────┐
│  ¿Ya autenticado?            │
└────────┬────────────────────┬─┘
         │ SÍ                 │ NO
         ▼                    ▼
    Redirect         ┌────────────────────┐
    a /              │ Muestra formulario │
                     └────────┬───────────┘
                              │
                              ▼
                     ┌────────────────────┐
                     │  Usuario rellena:  │
                     │  - Nombre          │
                     │  - Email           │
                     │  - Contraseña      │
                     └────────┬───────────┘
                              │
                              ▼
                     ┌────────────────────┐
                     │  POST /auth/       │
                     │  registro/         │
                     └────────┬───────────┘
                              │
                              ▼
                     ┌────────────────────┐
                     │  ¿Datos válidos?   │
                     └────┬────────────┬──┘
                         │ NO         │ SÍ
                         ▼            ▼
                    Errores    ┌────────────────────┐
                               │  Crear User        │
                               │  Crear Cliente     │
                               │  Crear Perfil     │
                               │  Agregar a grupo  │
                               │  "Cliente"        │
                               └────────┬───────────┘
                                        │
                                        ▼
                               ┌────────────────────┐
                               │  Redirect a /auth/ │
                               │  login/            │
                               └────────────────────┘
```

---

## Diagrama de Flujo - Crear Bicicleta (Protegida)

```
┌──────────────────────────┐
│ Usuario GET /crear/      │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ ¿user.is_authenticated?  │
└────┬────────────────┬────┘
     │ NO             │ SÍ
     ▼                ▼
Redirect        ┌────────────────────┐
a /auth/        │ ¿user en grupo     │
login/          │ "Personal"?        │
                └────┬────────────┬──┘
                     │ NO         │ SÍ
                     ▼            ▼
                403         ┌─────────────────────┐
             Forbidden      │ ¿Tiene permiso      │
                            │ add_bicicleta?      │
                            └────┬────────────┬───┘
                                 │ NO        │ SÍ
                                 ▼           ▼
                             403        Muestra
                          Forbidden    Formulario
                                        │
                                        ▼
                                 POST /crear/
                                        │
                                        ▼
                                 ¿Form válido?
                                 │          │
                                NO         SÍ
                                 │          ▼
                              Error   Crear Bicicleta
                                        │
                                        ▼
                                 Redirect a /
```

---

## Diagrama de Flujo - Estados de Usuario

```
┌───────────────────────────────────────────┐
│          ESTADOS DEL USUARIO              │
└───────────────────────────────────────────┘

       ┌─────────────────────┐
       │  Visitante (No Auth)│
       └──────────┬──────────┘
                  │
                  ├─ GET /            → Ver catálogo
                  ├─ GET /auth/login/ → Formulario login
                  ├─ GET /crear/      → Redirect a login
                  └─ GET /auth/perfil/→ Redirect a login
                  
       ┌─────────────────────────────┐
       │  Cliente (Autenticado)      │
       │  - grupo: "Cliente"         │
       └──────────┬──────────────────┘
                  │
                  ├─ GET /            → Ver catálogo (sin botones)
                  ├─ GET /auth/perfil/→ Ver perfil
                  ├─ GET /crear/      → 403 Forbidden
                  ├─ GET /actualizar/ → 403 Forbidden
                  └─ GET /eliminar/   → 403 Forbidden
                  
       ┌──────────────────────────────┐
       │  Personal (Autenticado)      │
       │  - grupo: "Personal"         │
       │  - permisos: add,change,     │
       │    delete,view               │
       └──────────┬───────────────────┘
                  │
                  ├─ GET /            → Ver catálogo (con botones)
                  ├─ GET /auth/perfil/→ Ver perfil
                  ├─ GET /crear/      → Formulario crear
                  ├─ GET /actualizar/ → Formulario editar
                  └─ GET /eliminar/   → Elimina y redirect
```

---

## Diagrama de Permisos

```
┌─────────────────────────────────────────────────────┐
│          PERMISOS POR GRUPO                         │
└─────────────────────────────────────────────────────┘

GRUPO: Cliente
├─ view_bicicleta      ✅ Puede VER el catálogo
├─ add_bicicleta       ❌ NO puede crear
├─ change_bicicleta    ❌ NO puede editar
└─ delete_bicicleta    ❌ NO puede eliminar

GRUPO: Personal
├─ view_bicicleta      ✅ Puede VER el catálogo
├─ add_bicicleta       ✅ Puede CREAR bicicletas
├─ change_bicicleta    ✅ Puede EDITAR bicicletas
└─ delete_bicicleta    ✅ Puede ELIMINAR bicicletas
```

---

## Modelo de Datos - Usuario

```
┌─────────────────────────────┐
│      User (Django Auth)     │
├─────────────────────────────┤
│ - id (PK)                   │
│ - username (unique)         │
│ - email (unique)            │
│ - first_name                │
│ - password (hashed)         │
│ - date_joined               │
│ - groups (M2M)              │◄────────┐
│ - user_permissions (M2M)    │         │
└─────────────────────────────┘         │
         ▲                              │
         │                              │
         │                       ┌──────┴─────────────┐
         │                       │   Grupo (Group)    │
         │                       ├────────────────────┤
         │                       │ - id (PK)          │
         │                       │ - name (unique)    │
         │                       │ - permissions (M2M)│
         │                       └──────┬─────────────┘
         │                              │
         │                    ┌─────────┴──────────┬──────────┐
         │            ┌───────▼────────┐   ┌──────▼────────┐
         │            │   Cliente      │   │  Permission  │
         │            ├────────────────┤   ├──────────────┤
         │            │ - nombre       │   │ - codename   │
         │            │ - email (FK)   │   │ - name       │
         │            │ - perfil (O2O) │   └──────────────┘
         │            └────────────────┘
         │
    ┌────┴───────────────────┐
    │  PerfilCliente         │
    ├────────────────────────┤
    │ - cliente (O2O, PK)    │
    │ - direccion            │
    │ - telefono             │
    │ - fecha_nacimiento     │
    └────────────────────────┘
```

---

## Flujo de Validación de Permisos

```
┌────────────────────────────────────────┐
│  Usuario intenta acceder a /crear/     │
└────────────────┬───────────────────────┘
                 │
                 ▼
    ┌───────────────────────────┐
    │ @login_required           │
    └───────┬────────┬──────────┘
           YES       NO
            │         └──────► Redirect /auth/login/
            │
            ▼
    ┌───────────────────────────┐
    │ @user_passes_test(        │
    │   es_personal)            │
    └───────┬────────┬──────────┘
           YES       NO
            │         └──────► 403 Forbidden
            │
            ▼
    ┌──────────────────────────────┐
    │ @permission_required         │
    │ ('add_bicicleta')            │
    └───────┬────────┬─────────────┘
           YES       NO
            │         └──────► 403 Forbidden
            │
            ▼
    ┌──────────────────────────────┐
    │ ✅ ACCESO CONCEDIDO          │
    │ Mostrar formulario crear     │
    └──────────────────────────────┘
```

---

## Decoradores de Protección

```python
# Opción 1: Solo autenticado
@login_required(login_url='login')
def mi_vista(request):
    pass

# Opción 2: Autenticado + Pertenece a grupo
@login_required
@user_passes_test(es_personal)
def vista_personal(request):
    pass

# Opción 3: Autenticado + Tiene permiso específico
@login_required
@permission_required('app_bicicletas.add_bicicleta')
def crear_bicicleta(request):
    pass

# Opción 4: Combinado (lo más seguro)
@login_required
@user_passes_test(es_personal)
@permission_required('app_bicicletas.add_bicicleta')
def crear_bicicleta_super_seguro(request):
    pass
```

---

## Variables de Contexto en Templates

```django
┌─────────────────────────────────────────────┐
│  Variables disponibles en templates         │
└─────────────────────────────────────────────┘

{{ user }}
    ├─ user.is_authenticated    → True/False
    ├─ user.username             → nombre_usuario
    ├─ user.email                → email@example.com
    ├─ user.first_name           → Juan
    ├─ user.groups.all           → [Cliente, Personal]
    ├─ user.has_perm('...')      → True/False
    ├─ user.is_staff             → True/False
    └─ user.is_superuser         → True/False

Ejemplo:
{% if user.is_authenticated %}
    Hola {{ user.first_name }}
{% else %}
    Por favor inicia sesión
{% endif %}
```

---

**Diagrama actualizado**: 19 de noviembre de 2025

